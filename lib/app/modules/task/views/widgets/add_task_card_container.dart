import 'package:flutter/material.dart';

class AddTaskCardContainer extends StatelessWidget {
  final Widget child;

  const AddTaskCardContainer({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16),
      decoration: BoxDecoration(
        color: Colors.white,
        borderRadius: BorderRadius.circular(12),
      ),
      child: child,
    );
  }
}
